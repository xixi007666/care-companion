# 发布为多人可访问网页（GitHub + Render）

这份说明对应当前的 **家庭照护小范围试用版**：支持 3–6 岁儿童连续行为观察，以及 60 岁以上老年人的日常状态与用药记录。它会把程序变成一个可从手机访问的网址：照护者注册自己的账号、填写自己的模型 Key、保存自己的加密记录。

> 这次线上发布会创建一套新的线上数据。不要把电脑里的 `.env` 或 `data` 文件夹上传到 GitHub，也不要把其中内容发给任何人。

## 发布前你需要准备

1. 一个 GitHub 账号。
2. 一个 Render 账号，并能开通可挂载数据盘的付费 Web Service。
3. 只邀请 20–50 个认识的儿童家庭或老年照护家庭做封闭试用；不要先公开到大群。

## 第 1 步：创建私有 GitHub 仓库

1. 打开 `https://github.com`，登录后点击右上角 `+`，选择 **New repository**。
2. Repository name 填 `care-companion-pilot`。
3. 选择 **Private**。
4. 不勾选 README、.gitignore 或 license，点击 **Create repository**。
5. 在新仓库页面点击 **Add file** → **Upload files**。
6. 把本项目文件夹里的所有内容拖进去上传。确认根目录能看见 `server.js`、`package.json`、`render.yaml`、`public` 文件夹和 `.gitignore`。
7. 页面下方点击 **Commit changes**。

`.gitignore` 会阻止 `.env`、`data` 与 SQLite 文件进入 Git；上传前仍要确认 GitHub 文件列表里没有它们。

## 第 2 步：从 GitHub 部署到 Render

1. 打开 `https://dashboard.render.com`，登录后点击 **New** → **Blueprint**。
2. 第一次使用时，按页面提示连接 GitHub；只授权刚创建的私有仓库。
3. 选择 `care-companion-pilot` 仓库。Render 会读到根目录的 `render.yaml`。
4. 确认服务配置：
   - 类型：Web Service
   - 地区：Singapore
   - 运行环境：Node
   - 启动命令：`npm start`
   - 数据盘路径：`/opt/render/project/src/data`
   - 数据盘大小：1 GB
5. 点击部署并等待状态变成 **Live**。
6. 打开 Render 给出的 `onrender.com` 网址。看到登录页即表示上线成功。

`render.yaml` 会让 Render 自动生成一个新的 `APP_ENCRYPTION_KEY`。它只属于线上服务，**不要下载、复制、分享或随意更换**；更换后旧数据无法解密。

## 第 3 步：上线验收

1. 用自己的邮箱注册一个全新的线上测试账号。
2. 保存档案，填写自己的 Kimi API 地址、模型名与 Key。
3. 新增三条记录，完成观察并生成 AI 报告。
4. 用手机无痕模式再打开该网址，确认能正常注册与登录。
5. 再注册一个测试账号，确认它看不到第一个账号的记录、报告、观察历史或模型配置。

线上数据库与电脑里的数据库是两套独立数据；你本机已有账号不会自动出现在网站上，这是正常的。

## 可以发给试用者的话

> 我在做一个家庭照护连续记录小工具：儿童场景可记录睡眠、情绪和日常情况；老年人场景可记录日常状态与用药时段。它不会做诊断或用药决策；你可以用自己的模型 Key 生成结构化观察。现在只招募少量家庭试用，请不要填写真实姓名、联系方式或上传照片。

## 当前边界

- 每位用户的模型 Key 由服务器加密保存，只用于该用户发起的模型调用，不会回显。
- 紧急事件记录不发送给 AI；有危险或快速加重的情况，先联系所在地的急救或医疗服务。
- 这仍是封闭试用版。正式公开前需补充清晰的隐私政策与监护人同意、账号删除/导出、备份恢复、邮箱验证和密码找回等能力。
