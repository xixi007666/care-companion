# 好好照护 · 可部署 BYOK 试运营版

这不是只展示 UI 的原型。它包含真实账号会话、照护记录持久化、服务端加密、每位用户独立的模型配置，以及实际向 OpenAI 兼容 API 发起的调用。

当前试运营版支持两个独立场景：

- **3–6 岁儿童**：首次 8 题家庭行为观察（非诊断）→ 结果分层 → 7 天照护计划 → 加密日常记录 → 后续连续观察与 AI 照护报告。
- **60 岁以上老年人**：首次 8 题日常状态与用药观察 → 7 天记录计划 → 睡眠、食欲、活动与日常自理记录 → 用药时段和“已按计划 / 未按计划 / 不确定”状态留存。

两个场景都会在首次答题建立基线；后续每轮保留 4 个固定对比题，再按上一轮的重点场景生成 3 个追问。每轮只与本账号的上一轮比较，不构成医学、发育或用药诊断。当前一个账号只管理一位照护对象，避免不同家人的记录混在一起。

> 适用范围：小规模封闭测评（例如 10–50 个受邀家庭）。在完成目标市场的隐私、未成年人健康信息、医疗内容、备份和安全审查之前，不要向公众开放。

## 架构与安全边界

```text
浏览器 → HTTPS 服务端 → 加密 SQLite 持久卷
                   └── 仅在调用时解密用户自己的 API Key → 用户选择的模型供应商
```

- 浏览器不保存 API Key；Key 不会被 `GET` 接口返回。
- 照护事件、档案、模型 Key 均以 AES-256-GCM 加密后写入数据库。
- AI 只接收去身份化的事件摘要：类别、情绪、时间段和关键词标签；不会收到原始备注。
- 连续观察的原始答案与每轮差异均加密保存；模型不会收到完整答题内容，只会使用既有的去身份化记录摘要。
- 多用户通过账号和服务端查询条件隔离；测试脚本验证用户 B 读不到用户 A 的记录。
- 药品照片上传与识别故意未实现。它需要独立授权、专用处理流与访问审计；即使未来接入，也只能辅助录入并必须由家属人工确认，不能用于自动给出服药或剂量建议。
- 所有模型返回都必须通过固定 JSON 结构和安全词过滤；命中紧急标签时，服务端不会调用模型，而是返回受控的升级提示。

## 一、启动本地试运营环境

要求：Node.js 22.5 或更高版本。项目没有第三方运行时依赖，不需要 `npm install`。

```bash
cd care-companion-live
cp .env.example .env
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

把最后一条命令输出的值完整填入 `.env` 的 `APP_ENCRYPTION_KEY`。不要更换已上线实例的密钥；否则无法解密原有记录。然后运行：

```bash
npm start
```

打开 `http://localhost:3000`，创建账号，在“设置”保存照护别名、场景、分层和自己的模型配置。

### 用户自己的模型配置

当前网关使用 OpenAI 兼容的 `POST {base_url}/chat/completions` 协议。每位用户填写：

| 字段 | 示例/规则 |
| --- | --- |
| API Base URL | 按供应商文档填写，通常以 `/v1` 结束；生产必须 HTTPS |
| 模型 ID | 按该用户供应商账号可用的模型名填写 |
| API Key | 仅提交一次到后端加密保存，不回显 |

平台不代用户承担推理费用；仍保留每人每小时 20 次调用上限，避免 Key 被滥用。可通过 `.env` 中的 `AI_CALLS_PER_HOUR` 调整。

## 二、无真实 Key 的端到端验收

项目包含一个仅限本机使用的 OpenAI 兼容假模型。它能验证真实服务链路，不会连接互联网，也不是模型能力测评。

```bash
cd care-companion-live
npm run test:e2e
```

这条测试会验证儿童的首次 8 题与后续 7 题连续观察、前后差异、7 天计划、加密记录、个人 Key 与模型报告；也会验证老年人专属题目、用药状态加密留存、以及不同账号之间的数据隔离。

## 三、用真实模型做封闭测评

1. 将 `.env` 的 `ALLOW_HTTP_LLM=false`，确保模型地址为 HTTPS。
2. 用受邀邮箱创建一个测试账号；不要共用管理员账号或 Key。
3. 在设置填写该测试账号自己的供应商地址、模型 ID 和 Key。
4. 记录至少 3 条同一周内事件，进入“AI 报告”点击生成。
5. 完成首次观察后，再完成一轮“连续观察”，确认页面显示与上一轮相比的变化，并确认新一轮同时含有“固定对比”和“上轮重点”题目。
6. 检查报告是否同时包含 **证据、局限、可执行行动和升级提示**；模型没有输出诊断、用药建议或紧急处置替代。
7. 用第二个测试账号验证：看不到第一账号的档案、事件、观察历史和模型配置。
8. 让测试用户在“记录”页删除一条事件，再刷新确认删除生效。

建议第一轮只测 10–20 个家庭、持续两周。核心指标：建档完成率、首日记录率、7 日连续记录率、报告打开率、行动计划完成率，以及不当 AI 建议的人工复核率。

## 四、Docker 部署

在服务器上安装 Docker Compose，并保证域名的 HTTPS 反向代理位于容器前方。服务器目录内：

```bash
cp .env.example .env
# 编辑 .env：填入随机 APP_ENCRYPTION_KEY，并将 COOKIE_SECURE=true
docker compose up -d --build
```

`docker-compose.yml` 会挂载持久卷保存 SQLite 数据。此版本必须维持单实例：SQLite 不适用于多个并发 Web 容器共用数据库。需要横向扩容、跨区高可用、灾备恢复或数百用户并发时，下一阶段应迁移到托管 PostgreSQL、密钥管理服务和对象存储。

## 五、上线前不可跳过的工作

- 使用 HTTPS，设置 `COOKIE_SECURE=true`，配置备份加密和恢复演练。
- 增加邮箱验证、密码找回、登录防暴力破解、管理员审计界面和异常告警。
- 为健康、未成年人和图片数据完成目标市场的合规审查、监护人同意、隐私政策、删除/导出流程及安全事件预案。
- 对应急卡和模型提示词进行专业审核、版本控制和回归测试。
- 迁移到生产数据库与密钥管理服务后，再考虑社群、图片、邀请协作者或专业人员角色。

本项目的 AI 提示和输出校验是产品安全底线，不是医疗服务或诊断系统。
